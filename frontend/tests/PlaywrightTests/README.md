Playwright C# tests

This folder contains a .NET Playwright test project used as examples.

To run locally:

1. Install the .NET SDK (6.0+) from https://dotnet.microsoft.com
2. From this folder run:

   dotnet restore
   dotnet test

Playwright browsers may need to be installed once via:

   dotnet tool install --global Microsoft.Playwright.CLI
   playwright install
